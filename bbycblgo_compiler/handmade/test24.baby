        IDENTIFICATION DIVISION.
        PROGRAM-ID. ARRAYLOOP.
        DATA DIVISION.
          01 ARR PICTURE IS 99 OCCURS 3 TIMES.
        PROCEDURE DIVISION.
          MOVE 10 TO ARR(1) ARR(2) ARR(3).
          LOOP VARYING I FROM 1 TO 3
            SUBTRACT 1 FROM ARR(I).
            DISPLAY ARR(I).
          END.
